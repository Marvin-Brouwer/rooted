registry=https://registry.npmjs.com/
shamefully-hoist=false
strict-peer-dependencies=false
shell-emulator=true
# Enable npm provenance attestations when publishing via CI
provenance=true
# Explicitly set registry to prevent a global .npmrc from overriding it
registry=https://registry.npmjs.com/
