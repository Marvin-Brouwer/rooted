{
	"name": "@rooted/monorepo",
	"private": true,
	"description": "rooted — web components rooted very close to the original",
	"scripts": {
		"prepare": "husky",
		"dev": "rooted-development development-runner examples/recipe-book",
		"build": "pnpm -r --stream run build",
		"build:dev": "pnpm -r --stream run build:dev",
		"build:ci": "pnpm -r run build:ci",
		"docs:dev": "vitepress dev docs",
		"docs:build": "vitepress build docs",
		"docs:preview": "vitepress preview docs",
		"test": "vp test",
		"test:watch": "vp test --watch",
		"lint:nofix": "oxlint -c .repo/config/oxlint/oxlint.config.json . && node .repo/config/oxlint/check-packages.mjs",
		"lint": "oxlint -c .repo/config/oxlint/oxlint.config.json --fix . && node .repo/config/oxlint/check-packages.mjs",
		"clean": "pnpm -r run clean",
		"release": "multi-semantic-release --ignore-private-packages",
		"full-update": "pnpm update -r"
	},
	"devDependencies": {
		"@commitlint/cli": "^21.2.1",
		"@commitlint/config-conventional": "^21.2.0",
		"@rooted/development": "workspace:*",
		"@rooted/pipeline": "workspace:*",
		"@types/unist": "^3.0.3",
		"eslint-plugin-perfectionist": "^5.9.0",
		"eslint-plugin-unicorn": "^64.0.0",
		"happy-dom": "^20.11.0",
		"husky": "^9.1.7",
		"multi-semantic-release": "^3.1.0",
		"oxlint": "^1.72.0",
		"tsdown": "^0.22.12",
		"typescript": "^6.0.3",
		"vite-plus": "^0.2.5",
		"vitepress": "^1.0.0",
		"vitest": "^4.1.10"
	},
	"engines": {
		"node": ">=22.0.0",
		"pnpm": ">=10.0.0"
	},
	"packageManager": "pnpm@11.0.9",
	"pnpm": {
		"peerDependencyRules": {
			"allowedVersions": {
				"vite-plugin-pwa>vite": "8"
			}
		},
		"overrides": {
			"source-map@0.8.0-beta.0": "0.7.4",
			"sourcemap-codec": "npm:@jridgewell/sourcemap-codec@^1.4.15"
		},
		"allowedDeprecatedVersions": {
			"glob": "11"
		},
		"patchedDependencies": {
			"semantic-release@25.0.3": "patches/semantic-release@25.0.3.patch"
		}
	}
}
