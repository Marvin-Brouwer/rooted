import { component } from '@rooted/components'

import styles from './content-notice.css'

export const ContentNotice = component({
	name: 'content-notice-page',
	styles,
	onMount({ append, create }) {
		append(create('div', {
			classes: styles.page,
			children: [
				create('h1', { textContent: 'About this demo' }),
				create('p', {
					classes: styles.lead,
					textContent: 'Recipe Book is an example application built to demonstrate the Rooted framework. It is not a real product and is not affiliated with any food publication or service.',
				}),

				create('h2', { textContent: 'Recipe content' }),
				create('p', {
					textContent: 'All recipes, descriptions, and associated metadata on this site are generated by claude.ai and are not real. Cooking times, ingredient quantities, and techniques have not been verified by a professional. Do not rely on this content for food safety or dietary advice.',
				}),

				create('h2', { textContent: 'Copyright concerns' }),
				create('p', {
					textContent: 'If you believe any content on this site reproduces copyrighted material, please follow these steps:',
				}),
				create('ol', {
					classes: styles.steps,
					children: [
						create('li', {
							children: [
								'Contact ',
								create('a', {
									href: 'https://support.anthropic.com',
									target: '_blank',
									rel: 'noopener noreferrer',
									textContent: 'Anthropic support',
								}),
								' first to report the issue and obtain a support ticket number.',
							],
						}),
						create('li', {
							children: [
								'Once you have your support ticket, open a ',
								create('a', {
									href: 'https://github.com/marvinh-/rooted/issues/new?template=demo-content-copyright.yml',
									target: '_blank',
									rel: 'noopener noreferrer',
									textContent: 'GitHub issue',
								}),
								' and attach your ticket number or link.',
							],
						}),
					],
				}),
				create('p', {
					classes: styles.note,
					textContent: 'Reporting to Anthropic first allows the underlying model behaviour to be reviewed and corrected at its source.',
				}),
			],
		}))
	},
})
