{
	"name": "@rooted/example-recipe-book",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"homepage": "https://marvin-brouwer.github.io/rooted/",
	"scripts": {
		"dev": "vite",
		"analyze": "vite build -- --minify --no-mangle --analyze",
		"analyze:dev": "vite build --mode development -- --no-mangle --analyze",
		"build": "pnpm build:ci",
		"build:dev": "vite build  --mode development -- --no-mangle --no-pwa",
		"build:ci": "vite build -- --minify",
		"preview": "vite preview"
	},
	"dependencies": {
		"@rooted/components": "workspace:^",
		"@rooted/router": "workspace:^",
		"@rooted/storage": "workspace:^",
		"@rooted/store": "workspace:^"
	},
	"devDependencies": {
		"@rooted/application": "workspace:^",
		"@varlock/vite-integration": "^1.1.1",
		"varlock": "^1.1.0",
		"gray-matter": "^4.0.3",
		"html-minifier-terser": "^7.2.0",
		"@types/html-minifier-terser": "^7.0.2",
		"marked": "^18.0.3",
		"typescript": "^6.0.3",
		"vite": "^8.0.10"
	}
}
