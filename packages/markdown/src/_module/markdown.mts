/**
 * Markdown content for the rooted framework. The `Markdown` component renders
 * HTML that the Vite plugin (`@rooted/markdown/vite`) produced at build time,
 * so no markdown parser ships to the browser.
 *
 *
 * - [Markdown guide](https://github.com/Marvin-Brouwer/rooted/blob/main/docs/guide/markdown.md)
 * - [Localization guide](https://github.com/Marvin-Brouwer/rooted/blob/main/docs/guide/localization.md)
 *
 * @module
 */

export { Markdown, type MarkdownModule, type MarkdownOptions, type MarkdownSource } from '../markdown.mts'
