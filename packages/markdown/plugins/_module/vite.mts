/**
 * The Vite plugin that turns `.md` files into modules with a `frontmatter` and
 * an `html` export.
 *
 *
 * - [Markdown guide](https://github.com/Marvin-Brouwer/rooted/blob/main/docs/guide/markdown.md)
 *
 * @module
 */

export { rootedMarkdown, type MarkdownPluginOptions } from '../markdown.mts'
