## API Report File for "@rooted/components"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ElementFactory } from '@rooted/elements';
import { EventBuilder } from '@rooted/elements/events';

// @public
export function a(constructor: r): n;

// @public (undocumented)
export function a<TOptions extends object>(constructor: r<TOptions>): n<TOptions>;

// @public
export type c = Record<string, u> & {
    readonly [l]: s;
};

// @public
export type d = Array<u> | u;

// @public
export function f(className: string | u, visible?: boolean | null | undefined): u;

// @public
export type i<TOptions extends object = never> = [TOptions] extends [never] ? BaseComponentContext : BaseComponentContext & {
    options: Readonly<TOptions>;
};

// @public
export const l: unique symbol;

// @public
export type n<TOptions extends object = never> = r<TOptions> & {
    readonly [componentBrand]: TOptions;
};

// @public
export function o(component: n): t;

// @public (undocumented)
export function o<TOptions extends object>(component: n<TOptions>, ...arguments_: object extends TOptions ? [options?: TOptions] : [options: TOptions]): t;

// @public (undocumented)
export function o<TComponent extends RootedElement>(component: RootedElementClass<TComponent>, properties: NoInfer<RootedElementProperties<TComponent>>): TComponent;

// @public
export type r<TOptions extends object = never> = {
    name: string;
    styles?: c;
    onMount(context: i<TOptions>): void | Promise<void>;
    [definedAt]?: string;
};

// @public
export type s = {
    readonly href: string; /** The `r` attribute value used to scope CSS to this component's element. */
    readonly scopeId: string;
};

// @public
export class t extends RootedElement {
    // (undocumented)
    protected onMount(): void;
    // (undocumented)
    protected onUnmount(): void;
    // (undocumented)
    static tagName: string;
}

// @public
export type u = string | undefined | null;

// (No @packageDocumentation comment for this package)

```
