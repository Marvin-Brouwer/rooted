## API Report File for "@rooted/router"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Component } from '@rooted/components';
import { createComponent } from '@rooted/components/elements';
import { TupleResult } from '@rooted/util';

// @public
export type a = (event: c) => void;

// @public
export class c extends CustomEvent<Error> {
    constructor(error: Error, route: UnknownRoute, href: string);
    // (undocumented)
    errorHandled: boolean;
    // (undocumented)
    readonly href: string;
    // (undocumented)
    readonly route: UnknownRoute;
}

// @public
export function i<const T extends RouterConfig>(config: r<T>): Component<n>;

// @public
export type l = {
    deploymentUrl?: string;
    defaultOgImage?: string;
    titleSuffix?: string;
};

// @public
export type n = {
    viewTransition?: boolean;
    scrollBehavior?: {
        scrollToTop?: 'on:start' | 'on:end' | 'on:start-and-end' | 'skip';
        saveScrollBeforeNavigate?: boolean;
        target?: Element;
    };
    on?: {
        navigate?: s;
        error?: a;
    }; /** Runtime SEO meta tag injection options. */
    seo?: l;
};

// @public
export class o extends CustomEvent<never> {
    constructor(navigationType: 'start' | 'end', href: string);
    // (undocumented)
    readonly href: string;
    // (undocumented)
    readonly navigationType: 'start' | 'end';
}

// @public
export type r<T extends RouterConfig> = { [K in keyof T]: K extends 'home' | 'notFound' ? T[K] : t<T[K]> };

// @public
export type s = (event: o) => void;

// @public
export type t<G> = G extends AnyRoute ? G : never;

// (No @packageDocumentation comment for this package)

```
