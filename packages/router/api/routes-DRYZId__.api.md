## API Report File for "@rooted/router"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { createComponent } from '@rooted/components/elements';
import { TupleResult } from '@rooted/util';

// @public (undocumented)
export type a = Number | String | Boolean | Date | Wildcard | R<any>;

// @public
export type b = NumberConstructor | StringConstructor | BooleanConstructor | DateConstructor | Wildcard;

// @public
export type c<T extends e[]> = (definition: {
    resolve: RouteResolver<T>;
}) => ExtractParent<T> extends never ? R<{
    parameters: FilterOutParent<T>;
}> : R<{
    parameters: FilterOutParent<T>;
    parent: ExtractParent<T>;
}>;

// @public
export type d<T extends R<any>> = {
    success: true;
    tokens: f<T>;
    length: number;
} | {
    success: false;
};

// @public (undocumented)
export type e = P | R<any>;

// @public (undocumented)
export type f<TRoute extends R<any>, D extends number = 10> = D extends 0 ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> : [TRoute[typeof routeMetadata]['parentType']] extends [never] ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> : TRoute[typeof routeMetadata]['parentType'] extends R<any> ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> & f<TRoute[typeof routeMetadata]['parentType'], RecursionCounter[D]> : Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>>;

// @public
export class g extends HrefBase {
    // (undocumented)
    static fromLocation(href: Location): g;
    // (undocumented)
    static fromString(href: string): g;
    // (undocumented)
    static fromUrl(href: URL): g;
    // (undocumented)
    get href(): string;
    // (undocumented)
    get pathOnly(): string;
}

// @public
export function h(): g;

// @public @deprecated (undocumented)
export function i(url: URL): U;

// @public @deprecated (undocumented)
export function i(location: Location): U;

// @public (undocumented)
export function i(href: U): U;

// @public (undocumented)
export function i(href: g): g;

// @public
export function i<TRoute extends R<any>>(...arguments_: keyof f<TRoute> extends never ? [route: TRoute, parameters?: f<TRoute>] : [route: TRoute, parameters: f<TRoute>]): g;

// @public
export function j(url: U, ...paths: g[]): U;

// @public (undocumented)
export function j(...paths: g[]): g;

// @public
export type k<T extends {
    parameters: any;
    parent?: any;
}> = {
    readonly tokenTypes: T['parameters'];
    readonly parentType: T extends {
        parent: infer P;
    } ? P : never;
    readonly hasParameterTokens: boolean;
    readonly hasWildcard: boolean;
    readonly hasErrors?: true;
    readonly routeParts: Array<string | e>;
    readonly staticRoute: false | string;
};

// @public
export type M = {
    target?: string | g | U | URL | Location;
    offset?: number;
    checkInclusive?: boolean;
};

// @public
export type P<K extends string = string, T extends a = a> = {
    key: K;
    type: T;
    match(parameter: string): TokenMatchResult<T>;
    [tokenBrand]: true;
};

// @public
export function p(href: string, includeBasePath?: boolean): g;

// @public (undocumented)
export type R<T extends RouteParameters<P[]>> = {
    readonly [routeMetadata]: k<T>;
    getMetadata(): Readonly<k<T>>;
    readonly resolve: RouteResolver<T['parameters']>;
    match(options?: M): Promise<d<R<T>>>;
};

// @public
export function r<const T extends e[]>(strings: TemplateStringsArray, ...values: T): c<T>;

// @public
export function t<K extends string = string, T extends b = b>(name: K, type: T): P<K, ParameterToTokenType<T>>;

// @public
export class U extends HrefBase {
    // (undocumented)
    static fromLocation(href: Location): U;
    // (undocumented)
    static fromString(href: string): U;
    // (undocumented)
    static fromUrl(href: URL): U;
    // (undocumented)
    get host(): string;
    // (undocumented)
    get hostName(): string;
    // (undocumented)
    get origin(): string;
    // (undocumented)
    get password(): string | undefined;
    // (undocumented)
    get path(): g;
    // (undocumented)
    get port(): number;
    // (undocumented)
    get protocol(): string;
    // (undocumented)
    get user(): string | undefined;
}

// @public
export function u(href: string): U;

// @public
export function w<K extends string = 'rest'>(name?: K): P<K, Wildcard>;

// (No @packageDocumentation comment for this package)

```
