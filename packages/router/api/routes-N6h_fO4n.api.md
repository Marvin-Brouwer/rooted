## API Report File for "@rooted/router"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { createComponent } from '@rooted/components/elements';
import { TupleResult } from '@rooted/util';

// @public (undocumented)
export type a<TRoute extends R<any>, D extends number = 10> = D extends 0 ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> : [TRoute[typeof routeMetadata]['parentType']] extends [never] ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> : TRoute[typeof routeMetadata]['parentType'] extends R<any> ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> & a<TRoute[typeof routeMetadata]['parentType'], RecursionCounter[D]> : Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>>;

// @public
export type b<K extends string = string, T extends d = d> = {
    key: K;
    type: T;
    match(parameter: string): TokenMatchResult<T>;
    [tokenBrand]: true;
};

// @public
export function c(): P;

// @public (undocumented)
export type d = Number | String | Boolean | Date | Wildcard | R<any>;

// @public
export type e = NumberConstructor | StringConstructor | BooleanConstructor | DateConstructor | Wildcard;

// @public @deprecated (undocumented)
export function f(url: URL): U;

// @public @deprecated (undocumented)
export function f(location: Location): U;

// @public (undocumented)
export function f(href: U): U;

// @public (undocumented)
export function f(href: P): P;

// @public
export function f<TRoute extends R<any>>(...arguments_: keyof a<TRoute> extends never ? [route: TRoute, parameters?: a<TRoute>] : [route: TRoute, parameters: a<TRoute>]): P;

// @public
export type g<T extends i[]> = (definition: {
    resolve: RouteResolver<T>;
}) => ExtractParent<T> extends never ? R<{
    parameters: FilterOutParent<T>;
}> : R<{
    parameters: FilterOutParent<T>;
    parent: ExtractParent<T>;
}>;

// @public
export type h<T extends R<any>> = {
    success: true;
    tokens: a<T>;
    length: number;
} | {
    success: false;
};

// @public (undocumented)
export type i = b | R<any>;

// @public
export function j(url: U, ...paths: P[]): U;

// @public (undocumented)
export function j(...paths: P[]): P;

// @public
export type k<T extends {
    parameters: any;
    parent?: any;
}> = {
    readonly tokenTypes: T['parameters'];
    readonly parentType: T extends {
        parent: infer P;
    } ? P : never;
    readonly hasParameterTokens: boolean;
    readonly hasWildcard: boolean;
    readonly hasErrors?: true;
    readonly routeParts: Array<string | i>;
    readonly staticRoute: false | string;
};

// @public
export type M = {
    target?: string | P | U | URL | Location;
    offset?: number;
    checkInclusive?: boolean;
};

// @public
export class P extends HrefBase {
    // (undocumented)
    static fromLocation(href: Location): P;
    // (undocumented)
    static fromString(href: string): P;
    // (undocumented)
    static fromUrl(href: URL): P;
    // (undocumented)
    get href(): string;
    // (undocumented)
    get pathOnly(): string;
}

// @public
export function p(href: string, includeBasePath?: boolean): P;

// @public (undocumented)
export type R<T extends RouteParameters<b[]>> = {
    readonly [routeMetadata]: k<T>;
    getMetadata(): Readonly<k<T>>;
    readonly resolve: RouteResolver<T['parameters']>;
    match(options?: M): Promise<h<R<T>>>;
};

// @public
export function r<const T extends i[]>(strings: TemplateStringsArray, ...values: T): g<T>;

// @public
export function t<K extends string = string, T extends e = e>(name: K, type: T): b<K, ParameterToTokenType<T>>;

// @public
export class U extends HrefBase {
    // (undocumented)
    static fromLocation(href: Location): U;
    // (undocumented)
    static fromString(href: string): U;
    // (undocumented)
    static fromUrl(href: URL): U;
    // (undocumented)
    get host(): string;
    // (undocumented)
    get hostName(): string;
    // (undocumented)
    get origin(): string;
    // (undocumented)
    get password(): string | undefined;
    // (undocumented)
    get path(): P;
    // (undocumented)
    get port(): number;
    // (undocumented)
    get protocol(): string;
    // (undocumented)
    get user(): string | undefined;
}

// @public
export function u(href: string): U;

// @public
export function w<K extends string = 'rest'>(name?: K): b<K, Wildcard>;

// (No @packageDocumentation comment for this package)

```
