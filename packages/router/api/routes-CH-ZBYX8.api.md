## API Report File for "@rooted/router"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { createComponent } from '@rooted/components/elements';
import { TupleResult } from '@rooted/util';

// @public
export type A = h<any> & M;

// @public
export type a = h<unknown> & M;

// @public
export type b = {
    target?: string | P | U | URL | Location;
    offset?: number;
    checkInclusive?: boolean;
};

// @public
export function c(): P;

// @public
export type d<K extends string = string, T extends e = e> = {
    key: K;
    type: T;
    match(parameter: string): TokenMatchResult<T>;
    [tokenBrand]: true;
};

// @public
export type E = h<RouteParameters<never>> & M;

// @public (undocumented)
export type e = Number | String | Boolean | Date | Wildcard | A;

// @public @deprecated (undocumented)
export function f(url: URL): U;

// @public @deprecated (undocumented)
export function f(location: Location): U;

// @public (undocumented)
export function f(href: U): U;

// @public (undocumented)
export function f(href: P): P;

// @public
export function f<TRoute extends A>(...arguments_: keyof R<TRoute> extends never ? [route: TRoute, parameters?: R<TRoute>] : [route: TRoute, parameters: R<TRoute>]): P;

// @public
export type g = NumberConstructor | StringConstructor | BooleanConstructor | DateConstructor | Wildcard;

// @public (undocumented)
export type h<T extends RouteParameters<d[]>> = {
    readonly [routeMetadata]: n<T>;
    getMetadata(): Readonly<n<T>>;
    readonly resolve: RouteResolver<T['parameters']>;
    match(options?: b): Promise<k<h<T>>>;
};

// @public
export type i<T extends l[]> = (definition: {
    resolve: RouteResolver<T>;
    seo?: m;
}) => ExtractParent<T> extends never ? h<{
    parameters: FilterOutParent<T>;
}> : h<{
    parameters: FilterOutParent<T>;
    parent: ExtractParent<T>;
}>;

// @public
export function j(url: U, ...paths: P[]): U;

// @public (undocumented)
export function j(...paths: P[]): P;

// @public
export type k<T extends A> = {
    success: true;
    tokens: R<T>;
    length: number;
} | {
    success: false;
};

// @public (undocumented)
export type l = d | A;

// @public
export type M = {
    match(): Promise<{
        success: boolean;
    }>;
};

// @public
export type m = {
    title?: string;
    description?: string;
    noIndex?: boolean;
    excludeFromSitemap?: boolean;
    image?: string;
    changeFrequency?: 'always' | 'hourly' | 'daily' | 'weekly' | 'monthly' | 'yearly' | 'never';
    priority?: number;
};

// @public
export type n<T extends {
    parameters: any;
    parent?: any;
}> = {
    readonly tokenTypes: T['parameters'];
    readonly parentType: T extends {
        parent: infer P;
    } ? P : never;
    readonly hasParameterTokens: boolean;
    readonly hasWildcard: boolean;
    readonly hasErrors?: true;
    readonly routeParts: Array<string | l>;
    readonly staticRoute: false | string;
    readonly seo?: m;
};

// @public
export class P extends HrefBase {
    // (undocumented)
    static fromLocation(href: Location): P;
    // (undocumented)
    static fromString(href: string): P;
    // (undocumented)
    static fromUrl(href: URL): P;
    // (undocumented)
    get href(): string;
    // (undocumented)
    get pathOnly(): string;
}

// @public
export function p(href: string, includeBasePath?: boolean): P;

// @public (undocumented)
export type R<TRoute extends A, D extends number = 10> = D extends 0 ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> : [TRoute[typeof routeMetadata]['parentType']] extends [never] ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> : TRoute[typeof routeMetadata]['parentType'] extends A ? Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>> & R<TRoute[typeof routeMetadata]['parentType'], RecursionCounter[D]> : Required<ConvertPathParameters<TRoute[typeof routeMetadata]['tokenTypes']>>;

// @public
export function r<const T extends l[]>(strings: TemplateStringsArray, ...values: T): i<T>;

// @public
export function t<K extends string = string, T extends g = g>(name: K, type: T): d<K, ParameterToTokenType<T>>;

// @public
export class U extends HrefBase {
    // (undocumented)
    static fromLocation(href: Location): U;
    // (undocumented)
    static fromString(href: string): U;
    // (undocumented)
    static fromUrl(href: URL): U;
    // (undocumented)
    get host(): string;
    // (undocumented)
    get hostName(): string;
    // (undocumented)
    get origin(): string;
    // (undocumented)
    get password(): string | undefined;
    // (undocumented)
    get path(): P;
    // (undocumented)
    get port(): number;
    // (undocumented)
    get protocol(): string;
    // (undocumented)
    get user(): string | undefined;
}

// @public
export function u(href: string): U;

// @public
export function w<K extends string = 'rest'>(name?: K): d<K, Wildcard>;

// (No @packageDocumentation comment for this package)

```
