/**
 * Boots a built rooted app in happy-dom and renders its static routes to HTML, at build time.
 *
 * `@rooted/adapter` runs this after the bundle is written. Node only, and not meant for app code.
 *
 * @module
 */

export * from '../renderer.mts'
