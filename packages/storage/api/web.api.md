## API Report File for "@rooted/storage"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public
type CookieInit_2<T = unknown> = Omit<globalThis.CookieInit, 'value'> & {
    value: T;
};
export { CookieInit_2 as CookieInit }

// @public
type CookieSameSite_2 = globalThis.CookieSameSite;
export { CookieSameSite_2 as CookieSameSite }

// @public
export type CookieStorage = {
    getItem(name: string): string | undefined;
    setItem(name: string, value: string): void;
    get<T = unknown>(name: string): T | undefined;
    set<T>(name: string, value: T): void;
    set<T>(init: CookieInit_2<T>): void;
    removeItem(name: string, options?: Pick<CookieInit_2, 'domain' | 'path'>): void; /** Every cookie name currently visible to `document.cookie`. */
    names(): string[]; /** Every cookie as a `Map<name, rawValue>`. */
    all(): Map<string, string>;
};

// @public
export const cookieStorage: CookieStorage;

// @public
export type LocalStorage = Pick<globalThis.Storage, 'length' | 'clear' | 'setItem' | 'removeItem'> & {
    getItem(key: string): string | undefined;
    key(index: number): string | undefined;
    get<T = unknown>(key: string): T | undefined;
    set<T>(key: string, value: T): void; /** All keys currently stored. Empty array under SSR. */
    keys(): string[];
};

// @public
const localStorage_2: LocalStorage;
export { localStorage_2 as localStorage }

// @public
export type SessionStorage = Pick<globalThis.Storage, 'length' | 'clear' | 'setItem' | 'removeItem'> & {
    getItem(key: string): string | undefined;
    key(index: number): string | undefined;
    get<T = unknown>(key: string): T | undefined;
    set<T>(key: string, value: T): void; /** All keys currently stored. Empty array under SSR. */
    keys(): string[];
};

// @public
const sessionStorage_2: SessionStorage;
export { sessionStorage_2 as sessionStorage }

// (No @packageDocumentation comment for this package)

```
